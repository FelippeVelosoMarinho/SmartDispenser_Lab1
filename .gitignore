node_modules

# Lock files (use bun.lock only)
package-lock.json
yarn.lock
pnpm-lock.yaml

# Python virtual environments
.venv/
__pycache__/
*.pyc

# Credentials — never commit these
firmware/eco-dispenser/secrets.h
firmware/.env
**/.env
backend/.env