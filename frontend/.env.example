# URL que o ESP32 recebe no pareamento BLE (heartbeat, scheduler).
# Use HTTP direto na API — não o ngrok do frontend.
# Exemplo em produção: http://SEU_SERVIDOR:8001
VITE_ESP_BACKEND_URL=http://2.24.216.183:8001
