# Slides — Pillar deck template

5-slide starter deck with the Pillar visual system: title, agenda, big-stat, pull-quote, closing. Uses `<deck-stage>` for keyboard nav, scaling, and one-slide-per-page print. Duplicate any `<section>` to add more slides.
